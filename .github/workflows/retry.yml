name: Oracle ARM Instance Retry
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  create-instance:
    runs-on: ubuntu-latest
    steps:
      - name: Install OCI dependencies
        run: pip install oci

      - name: Try to create Oracle instance
        env:
          OCI_USER: ${{ secrets.OCI_USER }}
          OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_TENANCY: ${{ secrets.OCI_TENANCY }}
          OCI_REGION: ${{ secrets.OCI_REGION }}
          OCI_KEY_CONTENT: ${{ secrets.OCI_KEY_CONTENT }}
          SUBNET_ID: ${{ secrets.SUBNET_ID }}
          IMAGE_ID: ${{ secrets.IMAGE_ID }}
          AVAILABILITY_DOMAIN: ${{ secrets.AVAILABILITY_DOMAIN }}
        run: |
          python3 - <<'EOF'
          import oci, os, sys
          config = {
            "user": os.environ["OCI_USER"],
            "fingerprint": os.environ["OCI_FINGERPRINT"],
            "tenancy": os.environ["OCI_TENANCY"],
            "region": os.environ["OCI_REGION"],
            "key_content": os.environ["OCI_KEY_CONTENT"]
          }
          client = oci.core.ComputeClient(config)
          details = oci.core.models.LaunchInstanceDetails(
            compartment_id=os.environ["OCI_TENANCY"],
            availability_domain=os.environ.get("AVAILABILITY_DOMAIN", "Onva:EU-PARIS-1-AD-1"),
            display_name="labelwise-oracle-vm",
            shape="VM.Standard.A1.Flex",
            shape_config=oci.core.models.LaunchInstanceShapeConfigDetails(ocpus=4, memory_in_gbs=24),
            source_details=oci.core.models.InstanceSourceViaImageDetails(
              source_type="image",
              image_id=os.environ["IMAGE_ID"]
            ),
            create_vnic_details=oci.core.models.CreateVnicDetails(
              subnet_id=os.environ["SUBNET_ID"],
              assign_public_ip=True
            )
          )
          try:
            result = client.launch_instance(details)
            print(f"SUCCESS! Instance OCID: {result.data.id}")
            sys.exit(0)
          except oci.exceptions.ServiceError as e:
            if "Out of host capacity" in str(e.message):
              print("Still out of capacity. Will retry in 5 minutes.")
              sys.exit(0)
            else:
              print(f"Error: {e}")
              sys.exit(1)
          except Exception as e:
            print(f"Error: {e}")
            sys.exit(1)
          EOF
